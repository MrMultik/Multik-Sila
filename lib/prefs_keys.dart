/// Имена ключей SharedPreferences, к которым обращается БОЛЬШЕ ОДНОГО файла.
///
/// Всё, что читает и пишет только главный экран, остаётся приватными
/// константами внутри него — сюда вынесено ровно то, что иначе пришлось бы
/// продублировать строкой.
///
/// Зачем отдельный файл: мастер первого запуска (`onboarding.dart`) заводит
/// профиль, а читает его потом `_loadProfiles` в main.dart. Пока имена были
/// написаны в двух местах руками, переименование в одном тихо ломало другое —
/// мастер сохранял бы профиль в ключ, которого никто не читает, и человек
/// после установки видел бы пустой список серверов без единой ошибки в логе.
library;

/// Список профилей подписок (JSON-массив `SubscriptionProfile`).
const String kProfilesPrefsKey = 'subscription_profiles';

/// Идентификатор активного профиля из списка выше.
const String kActiveProfilePrefsKey = 'active_profile_id';

/// Все настройки приложения одним JSON (`AppSettings`).
const String kSettingsPrefsKey = 'app_settings';

/// Мастер первого запуска пройден.
const String kOnboardingPrefsKey = 'onboarding_done';

/// Старая однопрофильная схема: одна ссылка на подписку без списка.
/// Используется только для миграции и для определения «установка не новая» —
/// новые записи сюда не делаются.
const String kLegacySubscriptionUrlKey = 'subscription_url';
