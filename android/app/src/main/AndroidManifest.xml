<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Туннель обязан пережить сворачивание приложения: иначе «включил и
         забыл» превращается в «работает, пока смотришь на экран». Отсюда
         служба переднего плана и постоянное уведомление. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <!-- Android 13+ спрашивает разрешение на уведомления отдельно. Без него
         служба переднего плана запустится, но человек не увидит ни состояния,
         ни кнопки «Отключить». -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Нужно для правил «эта программа — всегда мимо VPN». На Windows такие
         правила задаются путём к .exe, здесь — именем пакета, а чтобы показать
         человеку список установленных программ, их надо сначала увидеть:
         с Android 11 видимость пакетов ограничена по умолчанию. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"/>

    <application
        android:label="Multik Sila"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Служба туннеля.
             android:permission обязателен: Android соглашается отдавать
             интерфейс VPN только той службе, которую может вызвать лишь сама
             система. Без него VpnService.prepare() не найдёт наше приложение
             в списке возможных клиентов VPN.
             foregroundServiceType с Android 14 обязателен. Отдельного типа
             «vpn» в списке нет, поэтому берём specialUse и объявляем подтип
             свойством ниже — так делают все клиенты VPN. -->
        <service
            android:name=".SilaVpnService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn"/>
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
        </service>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
